pipeline {
  agent { label 'slave' }
  stages {
    stage('version') {
      steps {
        sh 'python --version'
      }
    }
    stage('script') {
      steps {
        sh 'python AppStoreScraper.py'
      }
    }
  }
}
